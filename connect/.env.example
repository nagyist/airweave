VITE_API_URL=http://localhost:8001

# CSP configuration (used by server middleware, not by Vite)
# CSP_FRAME_ANCESTORS: Origins allowed to embed Connect in an iframe.
# Default: * (all origins — auth is session-token based, not origin based)
# CSP_FRAME_ANCESTORS=https://app.example.com https://portal.example.com
#
# CSP_ADDITIONAL_CONNECT_SRC: Extra origins for API calls (space-separated).
# API_URL is always included automatically.
# CSP_ADDITIONAL_CONNECT_SRC=https://extra-api.example.com
