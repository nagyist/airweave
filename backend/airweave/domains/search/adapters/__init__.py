"""Adapters for the search module.

LLM, tokenizer, and reranker adapters have moved to airweave.adapters.{llm,tokenizer,reranker}.
Only vector_db remains here (search-specific, pending consolidation with sync pipeline).
"""
