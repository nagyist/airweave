---
description: Source contract v2 — what changed, why, and the exact patterns for migrating all 57 sources
alwaysApply: false
---

# Source Contract v2 — Migration Reference

## What this refactoring is

A ground-up rewrite of how source connectors receive their dependencies and handle errors. Every source in Airweave (~57 files) is being migrated from an implicit, setter-based architecture to explicit constructor injection with typed domain exceptions.

The refactoring spans 5 layers:
1. **Auth protocol hierarchy** — unified `SourceAuthProvider` abstraction
2. **HTTP helpers** — centralized response → domain exception translation
3. **Rate limiting** — refactored into injectable domain service
4. **BaseSource contract** — constructor DI, explicit method params
5. **Individual sources** — migrated one by one with `from_api()` entity patterns

## The problem (v1)

Sources had invisible, mutable state injected after construction via setters:

```python
# v1: dependencies appear magically after create()
instance = await SourceClass.create(access_token="...", config={})

instance.set_logger(logger)                    # set later
instance.set_token_provider(token_provider)    # set later
instance.set_http_client_factory(factory)      # set later
instance.set_cursor(cursor)                    # set later
instance.set_file_downloader(downloader)       # set later
instance.set_node_selections(selections)       # set later
```

This caused:
- **Silent `None` access** — if a setter was missed, `self.cursor` was `None` and crashed at runtime, not at construction
- **No type safety** — `create()` accepted `access_token: str` for OAuth sources, `credentials: AuthConfig` for PAT sources, and `credentials=None` for others — all with different conventions
- **Broad exception swallowing** — ~186 instances of `except Exception` across ~30 sources, silently eating 401s and producing "successful" syncs with 0 entities
- **Implicit HTTP client lifecycle** — sources created and destroyed `httpx.AsyncClient` via `async with self.http_client() as client:` on every method call, passing `client` through 3-5 layers of methods
- **Homebrew rate limiting** — e.g. Linear had a 180-line in-process sliding window tracker that assumed 1,200 req/hr (actual OAuth limit is 5,000)

## The solution (v2)

### Construction: all deps required at `__init__` time

```python
class AsanaSource(BaseSource):
    @classmethod
    async def create(
        cls,
        *,
        auth: TokenProviderProtocol,       # REQUIRED, typed
        logger: ContextualLogger,          # REQUIRED, typed
        http_client: AirweaveHttpClient,   # REQUIRED, typed
        config: AsanaConfig,               # REQUIRED, typed Pydantic model
    ) -> AsanaSource:
        return cls(auth=auth, logger=logger, http_client=http_client)
```

- **`auth`**: Always a `TokenProviderProtocol` (for 90%+ of sources) or `DirectCredentialProvider[T]` (for NTLM/structured creds). The lifecycle normalizes OAuth tokens, API keys, PATs, and auth provider credentials into this single interface. Sources call `await self.auth.get_token()` and never care how the token was obtained.
- **`logger`**: `ContextualLogger` with sync/search metadata already attached.
- **`http_client`**: Pre-built `AirweaveHttpClient` with SSRF protection and `SourceRateLimiter` already wired. No factory, no context manager.
- **`config`**: Typed Pydantic model (e.g. `AsanaConfig`, `LinearConfig`). The lifecycle parses raw `config_fields` from the DB into this.

### Operation: deps as method params, not `self`

```python
async def generate_entities(
    self,
    *,
    cursor: SyncCursor | None = None,
    files: FileService | None = None,
    node_selections: list[NodeSelectionData] | None = None,
) -> AsyncGenerator[BaseEntity, None]:
```

Cursor, file service, and node selections are **parameters**, not instance state. This makes sources stateless between sync runs and testable without full lifecycle setup.

### HTTP: single method, no client passing

**Before** — every source passed `client: httpx.AsyncClient` through 3-5 layers:
```python
async def _get_with_auth(self, client: httpx.AsyncClient, url, params):
    access_token = await self.get_access_token()
    response = await client.get(url, headers={"Authorization": f"Bearer {access_token}"}, ...)
    response.raise_for_status()  # raw httpx error, no domain context
    return response.json()
```

**After** — `self.http_client` is the pre-built client, `raise_for_status` translates to domain exceptions:
```python
async def _get(self, url: str, params=None) -> Dict:
    token = await self.auth.get_token()
    headers = {"Authorization": f"Bearer {token}"}
    response = await self.http_client.get(url, headers=headers, params=params)

    if response.status_code == 401 and self.auth.supports_refresh:
        new_token = await self.auth.force_refresh()
        headers = {"Authorization": f"Bearer {new_token}"}
        response = await self.http_client.get(url, headers=headers, params=params)

    raise_for_status(
        response,
        source_short_name=self.short_name,
        token_provider_kind=self.auth.provider_kind,
    )
    return response.json()
```

### Exceptions: typed hierarchy, not `except Exception`

`raise_for_status()` (in `platform/sources/http_helpers.py`) translates HTTP responses to domain exceptions:

| HTTP status | Domain exception | Source behavior |
|---|---|---|
| 401 | `SourceAuthError` | **Abort sync** — credentials dead |
| 403 | `SourceEntityForbiddenError` | Skip entity, log warning |
| 404 | `SourceEntityNotFoundError` | Skip entity, log warning |
| 429 | `SourceRateLimitError` | Retry with backoff (tenacity) |
| 5xx | `SourceServerError` | Retry with backoff (tenacity) |
| 3xx | `SourceError` | Unexpected redirect |

Critical rule: **`SourceAuthError` always propagates.** No `except SourceError` or `except Exception` may silently catch it. Every catch block in migrated sources has `except SourceAuthError: raise` before any broader catch.

### API quirks handled per-source

Some APIs don't follow HTTP conventions. These are handled in the source's `_get`/`_post` method **before** `raise_for_status`:

- **Linear**: Returns rate limits as HTTP 400 with GraphQL `RATELIMITED` error code (not 429). The source checks `errors[].extensions.code == "RATELIMITED"` and raises `SourceRateLimitError` with `retry_after` from `X-RateLimit-Requests-Reset` header.
- **Asana**: Returns 400 "result too large" when a paginated response exceeds internal limits. The source catches this via a private `_ResultTooLarge` exception and retries with smaller page sizes recursively.
- **Zoho**: Returns 400 with `"too many requests"` in the body for rate limits. Handled generically by `raise_for_status`'s `_is_rate_limit_disguised_as_400()`.
- **Bitbucket**: Standard 429 with `X-RateLimit-Limit` / `X-RateLimit-NearLimit` headers. 1,000–10,000 req/hr depending on endpoint + workspace size. Uses Basic Auth (email + API token), not Bearer.
- **Box**: Standard 429 with `retry-after` header. 1,000 req/min per user. Returns 403 for no-access and 404 for deleted items — the source returns `{}` for both (graceful skip).
- **Cal.com**: Standard 429. 120 req/min per API key (v2). Rate limit headers: `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`.
- **ClickUp**: Standard 429. 100 req/min per token. No API quirks — standard Bearer auth with `raise_for_status`.
- **Coda**: Standard 429. 100 read/6s, 4 list docs/6s. Removed homebrew in-process rate limiter (sliding window); now handled by `AirweaveHttpClient`.
- **Confluence**: Standard 429 via Atlassian Cloud. OAuth with rotating refresh. Requires accessible-resources lookup for `cloud_id` before API calls. Pages saved as HTML via `files.save_bytes()`.

### File downloads: FileService with raw exceptions

`FileService.download_from_url()` accepts the same `auth` and `client` that the source has. It handles 401 refresh internally for download streams. It raises raw `httpx` exceptions — the **source** decides what to do:

```python
async def _download_attachment(self, entity, files):
    try:
        await files.download_from_url(entity=entity, client=self.http_client, auth=self.auth, logger=self.logger)
        return entity if entity.local_path else None
    except FileSkippedException:
        return None           # intentional skip (unsupported type, too large)
    except httpx.HTTPStatusError as e:
        if e.response.status_code == 401:
            raise             # dead token → abort sync
        return None           # other HTTP errors → skip file
    # IOError/OSError propagate — infrastructure broken
```

### Entity construction: `from_api()` classmethods

Entity construction logic (field mapping, datetime parsing, breadcrumb building) moves from the source to the entity class.

#### When to add `from_api()`

Add it when the source has **>5 lines of field extraction** per entity constructor call. Skip it for entities where the source just passes API fields through 1:1 with no parsing (e.g. Box file entities with 30+ direct `.get()` calls that map to identically-named Pydantic fields — `from_api()` would just be `cls(**data)` with `extra="ignore"`).

#### Signature pattern

```python
@classmethod
def from_api(
    cls,
    data: Dict[str, Any],             # raw API response dict
    *,                                  # keyword-only after this
    breadcrumbs: List[Breadcrumb],      # parent context from caller
    project_gid: Optional[str] = None,  # any extra context the API doesn't provide
) -> MyEntity:
```

- First positional arg is always the raw API dict
- Parent context (breadcrumbs, IDs, names from parent entities) as keyword-only args
- Returns the entity instance (or `Optional[...]` if construction can fail, e.g. attachments without URLs)

#### What goes in `from_api()`

- **Datetime parsing**: `_parse_dt(data.get("createdAt"))` — put the helper at module level in the entities file
- **Field renaming**: camelCase → snake_case (`displayName` → `display_name`, `createdAt` → `created_time`)
- **Nested extraction**: `(data.get("team") or {}).get("name")` for nested API objects
- **Breadcrumb construction**: when the entity itself determines its breadcrumbs (e.g. `LinearTeamEntity` builds a self-referencing breadcrumb, `LinearProjectEntity` builds team breadcrumbs from `teams.nodes`)
- **Defaults and fallbacks**: `data.get("name") or data.get("slug") or entity_id`
- **URL construction**: `f"https://linear.app/team/{team_key}"`
- **Utility helpers**: `_find_primary_field(fields)` for Airtable tables, `_extract_record_name(record_id, fields)` for Airtable records — these live as module-level functions in the entities file

#### What stays in the source

- Orchestration (pagination, iteration, yielding)
- Error handling (try/except around API calls)
- Logging (`self.logger.debug(...)`)
- Conditional logic (skip archived issues, filter by file extensions)
- File downloads (these mutate the entity in place via `files.download_from_url`)

#### Example: before and after

**Before** — 25 lines per entity in the source:
```python
team_id = team.get("id")
team_name = team.get("name")
team_key = team.get("key")
parent = team.get("parent")
# ... 20 more lines of .get() calls ...
yield LinearTeamEntity(entity_id=team_id, breadcrumbs=breadcrumbs, name=team_name, ...)
```

**After** — 2 lines in the source, logic in entity:
```python
self.logger.debug(f"Processing team: {team.get('name')}")
yield LinearTeamEntity.from_api(team)
```

#### Entities with `from_api()` so far

| Entity file | Entities with `from_api()` | Module-level helpers |
|---|---|---|
| `entities/linear.py` | Team, Project, User, Issue, Comment (5) | `_parse_dt()`, `_team_breadcrumbs()` |
| `entities/asana.py` | Workspace, Project, Section, Task, Comment (5) | — (uses Pydantic `extra="ignore"` + `**data`) |
| `entities/airtable.py` | User, Base, Table, Record, Comment, Attachment (6) | `_find_primary_field()`, `_extract_record_name()` |
| `entities/box.py` | User, Folder, Comment, Collaboration (4) | — |
| `entities/calcom.py` | Booking, EventType, Schedule (3) | `_parse_iso8601()` |
| `entities/clickup.py` | Task, Comment, Subtask (3) | `_parse_clickup_ts()`, `_extract_comment_text()` |
| `entities/coda.py` | Doc, Page (2) | `_parse_dt()` |
| `entities/confluence.py` | Comment (1) | `_strip_html()` |
| `entities/google_docs.py` | Document (1) | `_parse_dt()` |
| `entities/google_slides.py` | Presentation (1) | `_parse_dt()` |
| `entities/google_drive.py` | FileEntity, FileDeletionEntity (2) | `_parse_drive_dt()`, `_GOOGLE_EXPORT_MAP` |
| `entities/hubspot.py` | Contact, Company, Deal, Ticket (4) | `_safe_float()` |
| `entities/intercom.py` | Conversation, ConversationMessage, Ticket (3) | `_parse_intercom_ts()`, `_strip_html()`, `_unwrap_list()` |
| `entities/jira.py` | Issue, ZephyrTestCase, ZephyrTestCycle, ZephyrTestPlan (4) | `_parse_datetime()`, `_extract_text_from_adf()` |
| `entities/monday.py` | Board, Item, Subitem, Update (4) | `_parse_monday_datetime()` |
| `entities/notion.py` | Database, Page, File (3) | `_extract_rich_text_plain()`, `_parse_notion_datetime()`, `_extract_page_title()`, `_format_database_schema()`, `_MIME_TYPE_MAP` |
| `entities/shopify.py` | ProductVariant, Customer, Order, DraftOrder, Collection, Fulfillment, GiftCard, File (8) | `_parse_shopify_ts()`, `_build_customer_breadcrumb()` |
| `entities/slab.py` | Topic, Post, Comment (3) | `_parse_dt()`, `_quill_delta_to_plain_text()`, `_json_description_to_string()`, `_build_topic_url()`, `_build_post_url()` |
| `entities/slack.py` | Message (1) | `_parse_slack_ts()` |
| `entities/slite.py` | Note (1) | `_parse_iso()` |
| `entities/stripe.py` | Charge, Invoice, PaymentIntent, PaymentMethod, Payout, Subscription (6) | `_parse_stripe_ts()` |

## Sources migrated so far

| Source | Lines before | Lines after | `from_api()` | Key changes |
|---|---|---|---|---|
| **Asana** | ~600 | 464 | 5 entities | Pilot migration. Recursive `_paginate` for "too large" 400s. |
| **Linear** | 1,148 | 701 | 5 entities | Removed homebrew rate limiter. GraphQL `RATELIMITED` detection. |
| **Airtable** | 603 | 378 | 6 entities | Removed manual 429 sleep-and-retry. |
| **Apollo** | 338 | ~338 | — | Contract migration (earlier commit). |
| **Attio** | 553 | ~553 | — | Contract migration (earlier commit). |
| **Bitbucket** | 649 | 496 | — | Basic Auth via `DirectCredentialProvider`. `files.save_bytes()` for code content. |
| **Box** | 704 | 440 | 4 entities | Removed homebrew rate limiter. 403/404 → graceful `{}` return. |
| **Cal.com** | 421 | 281 | 3 entities | Cursor-based incremental sync via param. Self-hosted `_base_url`. |
| **ClickUp** | 736 | ~540 | 3 entities | Removed debug file-reading. `files` param threaded to attachment downloads. Task/subtask/comment `from_api()`. |
| **Coda** | 383 | ~300 | 2 entities | Removed homebrew sliding-window rate limiter. Doc/page `from_api()` with `_parse_dt()`. |
| **Confluence** | 603 | ~500 | 1 entity | Fixed hardcoded `your-domain` URLs → `self._base_url`. Comment `from_api()` with `_strip_html()`. |
| **Google Docs** | 509 | 372 | 1 entity | `from_api()` on Presentation entity. Cursor via param. `_download_document()` extracted. |
| **Google Drive** | 1,628 | 1,448 | 2 entities | `_download_file()` helper. Cursor via `self._cursor`. File+Deletion `from_api()`. |
| **Google Slides** | 326 | 267 | 1 entity | `from_api()` on Presentation entity. `_download_presentation()` extracted. |
| **HubSpot** | 673 | 549 | 4 entities | All 4 object entities with `from_api()`. `_safe_float()` for deal amounts. |
| **Intercom** | 501 | 490 | 3 entities | Conversation+Message+Ticket `from_api()`. `_strip_html()`, `_unwrap_list()` in entities. |
| **Jira** | 1,015 | 689 | 4 entities | Atlassian OAuth (like Confluence). `_get_accessible_resources()` with 401 refresh. Zephyr API support preserved. |
| **Monday** | 729 | 707 | 4 entities | Board+Item+Subitem+Update `from_api()`. `_parse_monday_datetime()`. |
| **Notion** | 1,812 | 1,553 | 3 entities | Database+Page+File `from_api()`. 5 module-level helpers extracted. Removed homebrew rate limiter. |
| **Shopify** | 1,353 | 1,199 | 8 entities | `DirectCredentialProvider` for client_id/secret. `config.shop_domain` typed. `_build_customer_breadcrumb()` shared helper. |
| **Slab** | 662 | 336 | 3 entities | Removed homebrew rate limiter. GraphQL `_post()`. Topic+Post+Comment `from_api()`. |
| **Slack** | 358 | 303 | 1 entity | Message `from_api()` with `_parse_slack_ts()`. |
| **Slite** | 232 | 194 | 1 entity | Note `from_api()` with `_parse_iso()`. `config.include_archived` typed. |
| **Snapshot** | 541 | 538 | — | Internal replay source. `config.path` typed. No HTTP calls. |
| **Stripe** | 860 | 800 | 6 entities | Charge+Invoice+PaymentIntent+PaymentMethod+Payout+Subscription `from_api()`. Basic Auth via `base64`. |
| **Stub** | 934 | 933 | — | Internal test source. All `config.get()` → typed attrs. No HTTP calls. |

## Infrastructure changes (in this branch)

### `platform/sources/http_helpers.py` (new)
Centralized `raise_for_status()` — translates `httpx.Response` → domain exceptions. Requires `source_short_name` and `token_provider_kind` for every call.

### `platform/sources/retry_helpers.py` (updated)
`should_retry_on_rate_limit()` now recognizes `SourceRateLimitError`. `should_retry_on_server_error()` added for `SourceServerError`. `wait_rate_limit_with_backoff()` reads `retry_after` from the exception.

### `domains/sources/rate_limiting/` (refactored)
- `SourceRateLimiter` — injects `redis.asyncio.Redis` + `RateLimitConfigProvider` (no global singletons)
- `RateLimitConfig` dataclass, `InternalRateLimitExceeded` exception
- `DatabaseRateLimitConfigProvider` — fetches from DB, caches in Redis
- `AirweaveHttpClient` catches `InternalRateLimitExceeded` → synthetic HTTP 429 (transparent masquerade)

### `domains/sources/exceptions.py` (tightened)
- `SourceAuthError` requires `status_code`, `source_short_name`, `token_provider_kind` (all non-optional)
- `SourceEntityForbiddenError`, `SourceEntityNotFoundError` for per-entity 403/404
- `SourceRateLimitError` carries `retry_after: float`
- `SourceServerError` carries `status_code: int`

### `domains/storage/file_service.py` (simplified)
Raises raw `httpx` exceptions. No domain exception translation — that's the source's job. Handles 401 refresh internally for download streams.

### `platform/sources/_base.py` (v2 contract)
- `__init__` requires `auth`, `logger`, `http_client` (keyword-only)
- `create()` requires typed kwargs
- `generate_entities()` accepts `cursor`, `files`, `node_selections` as params
- Deprecated setters kept as no-ops during migration
- `self.get_access_token()` delegates to `self.auth.get_token()` for backward compat

## Execution checklist for remaining sources

For each source:

1. **`create()`** — keyword-only: `auth: TokenProviderProtocol`, `logger: ContextualLogger`, `http_client: AirweaveHttpClient`, `config: XConfig`
2. **`_get`/`_post`** — remove `client` param, use `self.http_client`, `self.auth.get_token()`, 401 refresh, `raise_for_status()`
3. **`generate_entities()`** — accept `cursor`, `files`, `node_selections` kwargs
4. **Remove `async with self.http_client() as client:`** — use `self.http_client` directly
5. **Exception handling** — `SourceAuthError: raise` before any broad `except`. Remove `except Exception` from data fetchers.
6. **File downloads** — `files.download_from_url(entity, client=self.http_client, auth=self.auth, logger=self.logger)`
7. **Entity construction** — add `from_api()` classmethods, simplify source process functions
8. **Remove dead code** — homebrew rate limiters, manual 429 handling, unused `_handle_http_error` methods
9. **API quirks** — research the specific API's rate limit behavior (some return 400 not 429), handle before `raise_for_status`

## What to remove after all 57 sources are migrated

- All deprecated setters on `BaseSource` (`set_logger`, `set_token_provider`, `set_http_client_factory`, `set_file_downloader`, `set_cursor`, `set_node_selections`)
- Deprecated properties (`cursor`, `file_downloader`, `token_provider`)
- `http_client_ctx()` context manager
- `_http_client_factory` backward-compat shim
- Positional `credentials` and `config` params on `create()`
- Change registry validation from warnings to errors
